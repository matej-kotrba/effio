import type { Actions } from "@sveltejs/kit"

export const actions: Actions = {
  deleteUsers: async ({ request }) => {
    // const formData = await request.formData()
    // console.log(formData)
    // const users = formData.values()
    // console.log(users)
  }
}